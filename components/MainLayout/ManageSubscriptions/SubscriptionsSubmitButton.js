import styles from '../../../styles/ManageSubscriptions/ManageSubscriptions.module.scss';

export const SubscriptionsSubmitButton = ({t}) => {

  return (
    <div className={styles.subscriptionsSubmitButton}>
      <p>
        update subscription
      </p>
    </div>
  )
}